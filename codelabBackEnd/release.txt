
docker buildx build --platform linux/amd64 -t apicodelaberp:latest . --load
docker buildx build --platform linux/amd64 -t apicodelaberp:1.0.0 . --load


# Ver logs
docker-compose logs -f

# Detener
docker-compose down

docker run -d -p 3000:3000 --name apicodelaberp-container apicodelaberp:latest


Por si toca hacer despliegue en la nube con Azure ACR
az login
az acr login --name "acrcodelaunah"
## nombre del container

docker tag apicodelaberp:latest acrcodelaunah.azurecr.io/apicodelaberp:latest
docker tag apicodelaberp:latest acrcodelaunah.azurecr.io/apicodelaberp:1.0.1
    `
docker push acrcodelaunah.azurecr.io/apicodelaberp:latest
docker push acrcodelaunah.azurecr.io/apicodelaberp:1.0.1
